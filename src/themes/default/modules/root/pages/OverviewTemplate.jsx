import React from 'react';
import { defaultTemplate } from 'core/utils';

function OverviewTemplate({}) {
  return <span>This page will be the home page of the login/app part</span>;
}

export default defaultTemplate(OverviewTemplate);
